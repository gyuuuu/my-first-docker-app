sudo: required

language: generic

services: 
  - docker

before_install:
  - echo "start creating an image with dockerfile"
  - docker build -t kai9702/docker-react-app -f Dockerfile.dev .

script:
  - docker run -e CI=true kai9702/docker-react-app npm run test -- --coverage

after_success:
  - echo "Test success"